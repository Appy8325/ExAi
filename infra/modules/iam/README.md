# `iam` module

Future purpose: IAM roles and policies for ECS task execution and task roles (`ecs-api`, `ecs-worker`), least-privilege access to Secrets Manager/SSM Parameter Store, and any CI/CD deployment roles, per [docs/43-security-architecture.md](../../../docs/43-security-architecture.md).

Consumed by the `ecs-api` and `ecs-worker` modules.

Not built out yet — this is Milestone 0 tooling scaffolding only. Real resource blocks land when the infra workstream begins in earnest.
