import type { ReactNode } from "react";

// Future shell: AttendeeShell (docs/13-application-layout.md §1) — Attendee
// App for Sofia, at /e/[eventSlug]/....
export default function AttendeeLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
