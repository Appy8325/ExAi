import type { ReactNode } from "react";

// Future shell: AdminShell (docs/13-application-layout.md §1) — Platform
// Admin for Alex Kim, at /admin/....
export default function AdminLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
