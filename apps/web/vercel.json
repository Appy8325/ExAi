{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm turbo run build --filter=web",
  "installCommand": "echo skip",
  "outputDirectory": ".next",
  "regions": ["iad1"],
  "github": {
    "silent": true
  }
}
