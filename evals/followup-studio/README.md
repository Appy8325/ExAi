# `evals/followup-studio`

Golden-set eval fixtures for Follow-up Studio, per [docs/21-ai-architecture.md](../../docs/21-ai-architecture.md) §5.

- **Content type:** 60 lead fixtures with expected follow-up draft output (tone, key points, grounded references back to the lead's notes/conversation history), used to grade the Follow-up Studio drafting feature.
- **Fixture count:** 60 lead fixtures.
- **File:** `golden.jsonl` -- one JSON object per line, one line per lead fixture.

Not populated yet -- this is Milestone 0 tooling scaffolding (`golden.jsonl` is present but empty). Real fixtures are authored when the Follow-up Studio feature milestone begins.
