{
  "name": "@concourse/flags",
  "version": "0.1.0",
  "private": true,
  "description": "FeatureFlagService \u2014 the sole PostHog Node SDK boundary (doc 34 \u00a73, doc 37 \u00a76.8). Mounted by apps/api and apps/worker.",
  "license": "UNLICENSED",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@concourse/shared": "workspace:*",
    "posthog-node": "^4.3.2"
  },
  "devDependencies": {
    "@concourse/config": "workspace:*",
    "typescript": "^5.7.2",
    "eslint": "^9.17.0",
    "vitest": "^2.1.8"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}
