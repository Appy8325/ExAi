{
  "name": "@concourse/notifications",
  "version": "0.1.0",
  "private": true,
  "description": "Templates + the sole SES/Web Push boundary (doc 33 \u00a71, \u00a77). @aws-sdk/client-ses and web-push are declared as dependencies only here \u2014 no other workspace may import them directly (doc 33 \u00a71, doc 37 \u00a76.7).",
  "license": "UNLICENSED",
  "exports": {
    ".": "./src/index.ts"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "dependencies": {
    "@concourse/shared": "workspace:*",
    "@aws-sdk/client-ses": "^3.700.0",
    "web-push": "^3.6.7",
    "@react-email/components": "^0.0.31",
    "@react-email/render": "^1.0.3"
  },
  "devDependencies": {
    "@concourse/config": "workspace:*",
    "@types/react": "^19.0.0",
    "@types/web-push": "^3.6.4",
    "typescript": "^5.7.2",
    "eslint": "^9.17.0",
    "vitest": "^2.1.8"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}
