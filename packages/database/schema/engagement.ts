import { sql } from "drizzle-orm";
import { boolean, check, index, integer, jsonb, pgTable, text, timestamp, uniqueIndex, uuid } from "drizzle-orm/pg-core";
import { eventExhibitors } from "./exhibitor";
import { events } from "./events-floor";
import { users } from "./identity";
const uuidv7 = sql`concourse.uuid_generate_v7()`;
export const leadForms = pgTable("lead_forms", { id: uuid("id").primaryKey().default(uuidv7), eventExhibitorId: uuid("event_exhibitor_id").notNull().references(() => eventExhibitors.id, { onDelete: "cascade" }), name: text("name").notNull(), description: text("description"), isDefault: boolean("is_default").notNull().default(false), status: text("status").notNull().default("active"), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() }, (t) => ({ nameUnique: uniqueIndex("lead_forms_exhibitor_name_key").on(t.eventExhibitorId, t.name), exhibitorIdx: index("lead_forms_event_exhibitor_id_idx").on(t.eventExhibitorId), statusCheck: check("lead_forms_status_check", sql`${t.status} IN ('active','archived')`) }));
export const leadFormFields = pgTable("lead_form_fields", { id: uuid("id").primaryKey().default(uuidv7), leadFormId: uuid("lead_form_id").notNull().references(() => leadForms.id, { onDelete: "cascade" }), key: text("key").notNull(), label: text("label").notNull(), type: text("type").notNull(), required: boolean("required").notNull().default(false), placeholder: text("placeholder"), defaultValue: jsonb("default_value"), validation: jsonb("validation").notNull().default(sql`'{}'::jsonb`), sortOrder: integer("sort_order").notNull(), helpText: text("help_text"), visibilityCondition: jsonb("visibility_condition"), status: text("status").notNull().default("active"), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() }, (t) => ({ keyUnique: uniqueIndex("lead_form_fields_form_key_key").on(t.leadFormId, t.key), orderUnique: uniqueIndex("lead_form_fields_form_order_key").on(t.leadFormId, t.sortOrder), formIdx: index("lead_form_fields_lead_form_id_idx").on(t.leadFormId), statusCheck: check("lead_form_fields_status_check", sql`${t.status} IN ('active','archived')`) }));
export const leadSubmissions = pgTable("lead_submissions", { id: uuid("id").primaryKey().default(uuidv7), eventId: uuid("event_id").notNull().references(() => events.id, { onDelete: "cascade" }), eventExhibitorId: uuid("event_exhibitor_id").notNull().references(() => eventExhibitors.id, { onDelete: "cascade" }), attendeeUserId: uuid("attendee_user_id").references(() => users.id, { onDelete: "set null" }), relationshipId: uuid("relationship_id").references(() => exhibitorRelationships.id, { onDelete: "restrict" }), leadFormId: uuid("lead_form_id").notNull().references(() => leadForms.id, { onDelete: "restrict" }), idempotencyKey: text("idempotency_key").notNull().default(sql`concourse.uuid_generate_v7()::text`), interactionSource: text("interaction_source").notNull(), potentialDuplicate: boolean("potential_duplicate").notNull().default(false), submittedAt: timestamp("submitted_at", { withTimezone: true }).notNull().defaultNow() }, (t) => ({ relationshipIdx:index("lead_submissions_relationship_submitted_at_idx").on(t.relationshipId,t.submittedAt), idempotencyUnique: uniqueIndex("lead_submissions_exhibitor_idempotency_key").on(t.eventExhibitorId, t.idempotencyKey), exhibitorSubmittedIdx: index("lead_submissions_exhibitor_submitted_at_idx").on(t.eventExhibitorId, t.submittedAt), duplicateIdx: index("lead_submissions_exhibitor_attendee_idx").on(t.eventExhibitorId, t.attendeeUserId), sourceCheck: check("lead_submissions_interaction_source_check", sql`${t.interactionSource} IN ('visitor_qr','exhibitor_device')`) }));
export const leadSubmissionValues = pgTable("lead_submission_values", { id: uuid("id").primaryKey().default(uuidv7), leadSubmissionId: uuid("lead_submission_id").notNull().references(() => leadSubmissions.id, { onDelete: "cascade" }), leadFormFieldId: uuid("lead_form_field_id").notNull().references(() => leadFormFields.id, { onDelete: "restrict" }), value: jsonb("value"), fieldSnapshot: jsonb("field_snapshot").notNull(), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow() }, (t) => ({ submissionFieldUnique: uniqueIndex("lead_submission_values_submission_field_key").on(t.leadSubmissionId, t.leadFormFieldId), submissionIdx: index("lead_submission_values_submission_id_idx").on(t.leadSubmissionId) }));
export const exhibitorRelationships = pgTable("exhibitor_relationships", { id: uuid("id").primaryKey().default(uuidv7), eventExhibitorId: uuid("event_exhibitor_id").notNull().references(() => eventExhibitors.id, { onDelete: "cascade" }), attendeeUserId: uuid("attendee_user_id").notNull().references(() => users.id, { onDelete: "cascade" }), status: text("status").notNull().default("active"), firstInteractionAt:timestamp("first_interaction_at",{withTimezone:true}).notNull().defaultNow(),latestInteractionAt:timestamp("latest_interaction_at",{withTimezone:true}).notNull().defaultNow(),interactionCount:integer("interaction_count").notNull().default(0),hasPotentialDuplicate:boolean("has_potential_duplicate").notNull().default(false),createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() }, (t) => ({ attendeeUnique: uniqueIndex("exhibitor_relationships_exhibitor_attendee_key").on(t.eventExhibitorId,t.attendeeUserId), exhibitorIdx:index("exhibitor_relationships_exhibitor_updated_idx").on(t.eventExhibitorId,t.updatedAt), statusCheck:check("exhibitor_relationships_status_check",sql`${t.status} IN ('active','archived')`) }));
export const exhibitorRelationshipNotes = pgTable("exhibitor_relationship_notes", { id: uuid("id").primaryKey().default(uuidv7), relationshipId: uuid("relationship_id").notNull().references(() => exhibitorRelationships.id,{onDelete:"cascade"}), body: text("body").notNull(), status:text("status").notNull().default("active"), createdByUserId:uuid("created_by_user_id").notNull().references(() => users.id,{onDelete:"restrict"}), createdAt:timestamp("created_at",{withTimezone:true}).notNull().defaultNow(), updatedAt:timestamp("updated_at",{withTimezone:true}).notNull().defaultNow() },(t)=>({relationshipIdx:index("exhibitor_relationship_notes_relationship_idx").on(t.relationshipId,t.createdAt),statusCheck:check("exhibitor_relationship_notes_status_check",sql`${t.status} IN ('active','archived')`)}));
