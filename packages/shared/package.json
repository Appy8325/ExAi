{
  "name": "@concourse/shared",
  "version": "0.1.0",
  "private": true,
  "description": "The universal leaf (doc 37 \u00a76.3, \u00a78 rule R3): Zod schemas, shared TS types, i18n resources, and cross-cutting constants. Zero internal workspace dependencies \u2014 every other workspace may import this, it imports none of them.",
  "license": "UNLICENSED",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@concourse/config": "workspace:*",
    "typescript": "^5.7.2",
    "eslint": "^9.17.0",
    "vitest": "^2.1.8"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}
